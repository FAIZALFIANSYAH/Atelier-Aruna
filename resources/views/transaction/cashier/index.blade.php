@extends('layouts.app')
@section('content')
<div class="management-page"><div class="management-shell"><div class="management-heading"><div><h1>Transaksi Masuk</h1><p>Proses pesanan pelanggan yang menunggu konfirmasi.</p></div></div><section class="management-card"><div class="management-card-head"><h2>Pesanan Pending</h2><span class="category-count">{{ $transactions->count() }} transaksi</span></div><table class="table modern-table"><thead><tr><th>Kode Transaksi</th><th>Pelanggan</th><th>Status</th><th>Tanggal</th><th>Aksi</th></tr></thead><tbody>@forelse($transactions as $transaction)<tr><td><strong>{{ $transaction->transaction_code }}</strong></td><td>{{ $transaction->member->name }}</td><td><span class="status-pill status-pending">Pending</span></td><td>{{ $transaction->created_at->format('d M Y, H:i') }}</td><td><form action="{{ route('cashier.process', $transaction->id) }}" method="POST" class="d-inline">@csrf @method('PATCH')<button class="btn btn-primary btn-sm"><i class="fas fa-check mr-1"></i> Proses</button></form></td></tr>@empty<tr><td colspan="5"><div class="empty-state"><i class="fas fa-cash-register"></i>Tidak ada transaksi yang menunggu diproses.</div></td></tr>@endforelse</tbody></table></section></div></div>
@endsection
