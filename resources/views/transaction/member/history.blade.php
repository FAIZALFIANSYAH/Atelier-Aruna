@extends('layouts.app')
@section('content')
<div class="management-page"><div class="management-shell"><div class="management-heading"><div><h1>Riwayat Transaksi</h1><p>Lihat status dan detail seluruh pesanan Anda.</p></div></div><section class="management-card"><div class="management-card-head"><h2>Pesanan Saya</h2><span class="category-count">{{ $transaction->count() }} transaksi</span></div><table class="table modern-table"><thead><tr><th>Kode</th><th>Produk</th><th>Status</th><th>Total</th><th>Tanggal</th><th>Aksi</th></tr></thead><tbody>@forelse($transaction as $item)<tr><td><strong>{{ $item->transaction_code }}</strong></td><td>{{ $item->transactionDetails->first()?->product?->name ?? '-' }} @if($item->transactionDetails->count() > 1)<small class="text-muted">+{{ $item->transactionDetails->count() - 1 }} lainnya</small>@endif</td><td><span class="status-pill status-{{ $item->status }}">{{ ucfirst($item->status) }}</span></td><td><strong>Rp {{ number_format($item->transactionDetails->sum('subtotal'), 0, ',', '.') }}</strong></td><td>{{ $item->created_at->format('d M Y') }}</td><td><a href="{{ route('transaction.show', $item->id) }}" class="btn btn-light border btn-sm">Detail</a>@if($item->status === 'pending')<form action="{{ route('transaction.cancel', $item->id) }}" method="POST" class="d-inline" onsubmit="return confirm('Batalkan transaksi ini?')">@csrf @method('PATCH')<button class="btn btn-danger btn-sm ml-1">Batal</button></form>@endif</td></tr>@empty<tr><td colspan="6"><div class="empty-state"><i class="fas fa-receipt"></i>Belum ada riwayat transaksi.</div></td></tr>@endforelse</tbody></table></section></div></div>
@endsection
