@extends('layouts.public')
@section('content')
<section class="public-hero"><div class="public-container public-hero-content"><span class="public-eyebrow">Atelier Aruna · Pakaian Jahitan</span><h1>Pakaian yang dijahit untuk terasa lebih personal.</h1><p>Jelajahi koleksi pakaian siap pakai atau pesan karya jahitan custom dengan ukuran, bahan, dan detail sesuai kebutuhan Anda.</p><a href="{{ route('transaction.index') }}" class="btn btn-light mt-2">Jelajahi Katalog <i class="fas fa-arrow-right ml-1"></i></a></div></section>
<section class="public-section"><div class="public-container"><h2>Kategori Pakaian</h2><p class="public-section-intro">Temukan model jahitan yang paling sesuai untuk Anda.</p><div class="public-category-grid">@forelse($categories as $category)<a class="public-category" href="{{ route('transaction.index', ['category' => $category->id]) }}"><i class="fas fa-tshirt"></i>{{ $category->name }}<small>{{ $category->product_count }} produk tersedia</small></a>@empty<p class="text-muted">Kategori belum tersedia.</p>@endforelse</div></div></section>
<section class="public-section pt-0"><div class="public-container"><div class="d-flex justify-content-between align-items-end"><div><h2>Pilihan Terbaru</h2><p class="public-section-intro">Koleksi yang baru ditambahkan oleh atelier kami.</p></div><a href="{{ route('transaction.index') }}" class="catalog-back-link mb-3">Lihat semua</a></div><div class="public-product-grid">@forelse($featuredProducts as $product)<article class="public-product"><div class="public-product-image">@if($product->image)<img src="{{ asset('storage/'.$product->image) }}" alt="{{ $product->name }}">@else<i class="fas fa-tshirt"></i>@endif</div><div class="public-product-body"><span class="badge badge-info">{{ $product->category->name }}</span><h3>{{ $product->name }}</h3><p>{{ $product->material ?: 'Pakaian jahitan pilihan' }}</p><div class="d-flex justify-content-between align-items-center"><span class="public-product-price">Rp {{ number_format($product->price, 0, ',', '.') }}</span><a href="{{ route('product.detail', $product) }}" class="catalog-detail-link">Detail</a></div></div></article>@empty<p class="text-muted">Produk belum tersedia.</p>@endforelse</div></div></section>
@endsection
